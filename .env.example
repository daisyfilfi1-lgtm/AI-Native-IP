# 默认不要设置：浏览器使用同源 /api/v1，由 next.config.js rewrites 在服务端转发到 Railway（无 CORS、无 Netlify 边缘 502）。
# 仅当需要「浏览器直连」某后端时，再设为完整 URL，例如：
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api/v1
#
# 若设置 NEXT_PUBLIC_API_MODE=direct，登录请求会直连 Railway（须与后端 CORS 一致）。
# 测试账号：手机号 + 验证码 123456（字段 code；后端也接受 password）。Railway 需 SMS_PROVIDER=mock 或 OTP_BYPASS_ENABLED=true。

# 可选：生产环境（Netlify）浏览器直连 Railway，绕过边缘 /api 代理，减轻素材录入轮询 502。
# 仅填 origin，无路径，须 https。构建时注入，与 netlify.toml 中 Railway 域名保持一致即可。
# NEXT_PUBLIC_API_DIRECT_ORIGIN=https://ai-native-ip-production.up.railway.app

# 服务端转发目标（Netlify / 本地 build 可选；默认见 next.config.js）
# RAILWAY_API_ORIGIN=https://ai-native-ip-production.up.railway.app

# 创作者工作台默认 IP（与后端 ip.ip_id 一致；小敏常见为 xiaomin1 或 xiaomin）。也可在页面「当前创作 IP」里切换并写入本地。
# NEXT_PUBLIC_CREATOR_IP_ID=xiaomin1
